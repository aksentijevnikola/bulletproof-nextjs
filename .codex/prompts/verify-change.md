# Verify a change

Start with focused tests and checks for the changed subsystem. Then run `bun run verify`; run `bun run e2e` for UI or routing changes. Report exact commands, exit status, failures, and checks not run.
