# Implement a feature

Read `AGENTS.md`, `docs/architecture.md`, and `docs/conventions.md`. Inspect the closest source and tests, state the intended layer, implement the smallest complete behavior, add behavior-focused tests, and run narrow relevant checks. Do not stage or commit unless asked.
