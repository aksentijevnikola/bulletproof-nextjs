# Project skills

- `project-architecture`: route code and import placement
- `project-verification`: narrow-to-full verification workflow
- `ui-placement`: decide whether UI belongs in shared, features, widgets, or app

Use the smallest relevant skill. Framework and library APIs must be checked against current official documentation.
