# Codex setup

Read `AGENTS.md`, then only the canonical document relevant to the task. Project Codex configuration deliberately avoids model, provider, authentication, and machine-specific settings.

Package installation, production builds, browser installation, external writes, and deployment changes follow the approval rules active in the user environment.

Reusable prompts live under `.codex/prompts`; focused project workflows live under `.agents/skills`.
