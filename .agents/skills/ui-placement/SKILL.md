---
name: ui-placement
description: Place Baseframe UI in shared, feature, widget, or route layers.
---

# UI placement

Read `docs/conventions.md`. Business-agnostic primitives belong in `src/shared/ui`, workflows in `src/features`, page-level composition in `src/widgets`, and route adapters in `src/app`.
